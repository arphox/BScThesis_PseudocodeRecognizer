program_kezd

// Hogy ne legyen probléma az újradeklarálással, felvettem itt mindet:
egész N 
egész[] tömb
egész összeg
egész darab
egész maxi

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."


// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."


// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

//  Összegzés
összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

// Megszámlálás
darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."

// Maximumkiválasztás
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

// Létrehozás
N = 10
tömb = létrehoz(egész)[N]

// Feltöltés
ciklus egész i = 0-tól i < N-ig
	tömb[i] = i * 10
ciklus_vége

ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."

összeg = 0
ciklus egész i = 0-tól i< N-ig
	összeg = összeg + tömb[i]
ciklus_vége
kiír "A tömb elemeinek összege ".összeg

darab = 0
ciklus egész i = 0-tól i< N-ig
	ha tömb[i] mod 2 == 0 akkor
		darab = darab + 1
	elágazás_vége
ciklus_vége
kiír: "A tömbben ".darab." darab páros elem van."
maxi = 0
ciklus egész i=1-től i<N-ig
	ha tömb[i] > tömb[maxi] akkor
		maxi = i
	elágazás_vége
ciklus_vége
kiír: "A tömb legnagyobb eleme: tömb[" . maxi . "] = " . tömb[maxi] . "."
program_vége